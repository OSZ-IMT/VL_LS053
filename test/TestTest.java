import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

/**
 * Ignorieren Sie solange diese Datei, bis ihre Lehrkraft was dazu sagt.
 */
class TestTest {

    /**
     * Ignorieren Sie solange diese Datei, bis ihre Lehrkraft was dazu sagt.
     * Tests sind ein optionales Zusatzfeature, welches nicht in jeder Klasse genutzt wird.
     * Beispieltest, der nur testet ob Tests funktionieren.
     */
    @Test
    void test() {
        assertTrue(true);
    }
}